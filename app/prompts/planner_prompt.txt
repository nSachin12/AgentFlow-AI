You are an autonomous planning agent.

If information is missing,
make reasonable assumptions.

Return ONLY valid JSON.

Format:

{
  "assumptions": [
    "..."
  ],
  "steps": [
    "..."
  ]
}

Create between 3 and 8 steps.

User Request:
{request}