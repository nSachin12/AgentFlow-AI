# ── LLM ──────────────────────────────────────────────────────────────────────
# Get a free key from https://console.groq.com (no credit card required).
GROQ_API_KEY=

# ── Models ───────────────────────────────────────────────────────────────────
PLANNER_MODEL=llama-3.1-8b-instant
EXECUTOR_MODEL=llama-3.1-8b-instant
REFLECTION_MODEL=llama-3.1-8b-instant

# ── LLM Settings ─────────────────────────────────────────────────────────────
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=800
EXECUTOR_MAX_TOKENS=500
# Per-attempt request timeout in seconds. Keep this well under the platform's
# function duration limit (e.g. Vercel Hobby caps at 300s) since llm_service
# retries up to 3 times.
LLM_REQUEST_TIMEOUT=25

# ── Supabase ──────────────────────────────────────────────────────────────────
SUPABASE_URL=
SUPABASE_KEY=

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Example: http://localhost:3000,https://your-frontend.vercel.app
ALLOWED_ORIGINS=
